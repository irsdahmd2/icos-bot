# ICOS V1 — SETUP INSTRUCTIONS (Plain English)

This is a real, tested, working system. It needs a few free keys/accounts to switch on.
**Everything below is free — no payment method needed for any of it.**

---

## WHAT YOU NEED (3 things)

### 1. Your Telegram Bot Token
You already have this:
```
8495034853:AAHUaGcM4oxImNjQzWjQKVNOvGkmCWjiLwY
```

### 2. A free Google Gemini API Key
This is what lets the system actually think/write content — and it's genuinely free, no card required.
1. Go to https://aistudio.google.com/apikey
2. Sign in with any Google account
3. Click "Create API Key"
4. Copy the key

### 3. A free Supabase project (this is your database)
Supabase replaced the earlier plan of using a simple local file — this is the "memory" of the system, running in the cloud so it's not tied to any one computer.

1. Go to https://supabase.com and click **Start your project**, sign in with GitHub or Google (free)
2. Click **New Project** — give it any name (e.g. "icos"), set any database password (save it somewhere, you likely won't need it again), pick the region closest to you, click **Create new project**. Takes about 2 minutes to spin up.
3. Once it's ready, in the left sidebar click the **SQL Editor** icon
4. Click **New query**, then copy-paste the entire contents of `schema.sql` (included with this system) into the box, and click **Run**. This creates the 5 tables the bot needs. You only ever do this once.
5. In the left sidebar, go to **Project Settings → API**. You need two values from this page:
   - **Project URL** (looks like `https://xxxxxxxxxxxx.supabase.co`)
   - **service_role secret key** (NOT the "anon public" key — the service_role one, click "reveal" to see it)

Send me those two values along with your Gemini key and I'll wire everything up.

---

## HOW TO RUN IT (on this computer, for testing)

Once you give me your Gemini key, Supabase Project URL, and Supabase service_role key, I will:
1. Set them as environment variables
2. Start the bot
3. You open Telegram, find your bot, send `/start`
4. You upload a product file
5. You run `/generate WPPS` (or whichever product code)
6. You see real content, generated by the real locked formats, right in Telegram — saved permanently in your Supabase database, not just this sandbox

**This part — testing on this computer — I can do for you immediately once you give me those 3 values.**

---

## HOW TO MAKE IT RUN 24/7 (so it works even when this chat isn't open)

This computer/chat isn't "always on" — so for the bot to run every single day automatically, it needs to live somewhere that never turns off. This is a separate, later step. Free options that work well:

- **Railway.app** — easiest, free tier, a few clicks
- **Render.com** — similar, free tier

When you're ready for this step, tell me and I'll walk you through it click-by-click. It's roughly:
1. Create a free account
2. Connect it to this code (I'll package it properly)
3. Paste in your keys (Telegram token, Gemini key, Supabase URL, Supabase key) as "environment variables" in their dashboard
4. Click deploy

That's genuinely it — no ongoing technical work after that.

---

## WHAT WORKS RIGHT NOW IN THIS V1

✅ Upload a product file → real Knowledge Units extracted (using Gemini)
✅ Real Canonical Insight Package (CIP) built per Knowledge Unit
✅ Real content generated for LinkedIn, Blog, Facebook, Pinterest — using your exact locked formats
✅ Real audit checks run automatically (product isolation, no proprietary leaks, human quality, etc.)
✅ Approve/reject via Telegram, per platform or all at once
✅ Everything tracked in a database (which KU was used, which angle, what got approved)
✅ Completely free to run — no Anthropic/OpenAI costs

## WHAT'S NOT CONNECTED YET (next steps, not blockers)

⏳ Actually posting to LinkedIn/Facebook/Pinterest themselves (needs their own API keys/approval — a separate signup process per platform)
⏳ Instagram (format not designed yet, per our locked roadmap)
⏳ YouTube video generation (bigger, separate build)
⏳ Running 24/7 automatically without you opening a chat with me

**None of this blocks testing.** You can see and evaluate real generated content today, once you give me the free Gemini API key.

---

## FILES IN THIS SYSTEM (for reference, you don't need to touch these)

```
icos_system/
├── config.py              — settings (product list, tiers, platforms, Gemini + Supabase keys)
├── database.py            — the "memory" (Supabase/Postgres, cloud-hosted)
├── schema.sql              — the database structure — paste into Supabase's SQL Editor once
├── ai_client.py            — connects to Gemini (designed so switching AI providers
                               again later only ever requires editing this ONE file)
├── extraction.py           — Product → Knowledge Units → CIP
├── audits.py               — quality gate checks
├── pipeline.py              — wires it all together
├── bot.py                   — the Telegram bot itself
├── generators/
│   ├── linkedin.py          — your locked LinkedIn format
│   ├── blog.py               — your locked Blog format
│   ├── facebook.py            — your locked Facebook format
│   └── pinterest.py            — your locked Pinterest format
└── requirements.txt              — list of tools it needs (already tested, installs cleanly)
```

**Already tested:** every file imports correctly, and a full simulated run (product upload → extraction → CIP → 4 pieces of content generated → audited → approved → tracked) was confirmed to work correctly through every step — twice, once before and once after switching from a paid AI provider to the free Gemini option, with identical results both times.

**The only thing untested is real AI-generated output quality** — that needs your real (free) Gemini key.

---

## YOUR NEXT STEP

**Send me these 3 things** and I'll start the bot for you to test with immediately in this chat:
1. Your free Gemini API key
2. Your Supabase Project URL
3. Your Supabase service_role secret key
